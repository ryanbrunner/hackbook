<tr>
  <td><%= post.user.name %></td>
  <td><%= post.body %></td>
  <td><%= time_ago_in_words(post.created_at) %> ago</td>
  <td>
    <% if post.user_id == current_user.id %>
      <%= link_to 'Remove', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <% end %>
</tr>
